1. Spin up a Node.js-driven Server(port 3000)
2. Handle two Routers: "/" and "/users"
  Return some greeting text on "/"
  Return a dummy list of users on "/users"
3. Add a form with a username input to the "/" page and submit a POST request to "/create-users" upon a button click
4. Add the "/create-user" route and parse the incoming data (username) and simply log it to the console